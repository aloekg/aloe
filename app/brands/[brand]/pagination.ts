// A constant, not an argument: the action's arguments are a cache key an anonymous caller writes.
export const BRAND_PAGE_SIZE = 24;

// Bounds what a public POST can mint in the Data Cache.
export const BRAND_MAX_PAGE = 200;
